// This file contains the configuration for the frontend application.
// It defines the URL for the backend API.

// For local development, use: 'http://localhost:3001/api/shapes/recognize'
// For production, replace with your live Render API URL.
const API_URL = 'https://your-recognition-service-url.onrender.com/api/shapes/recognize';
